/// @description Insert description here
// You can write your code in this editor
if (Hover) {
	draw_sprite(ExitButtonHover, ExitButtonHover, x, y);
} else {
	draw_sprite(ExitButton, ExitButton, x, y);
}